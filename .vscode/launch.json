{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/Backend/Barramento de eventos/package.json",
                "localRoot": "${workspaceFolder}/Backend/Barramento de eventos"
            }
        }
    ]
}